<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.spring.mapper.WorkspaceMapper"> 
   <insert id="insertWorkspace">
      insert into user values(#{workspaceNo}, #{master}, #{title}, #{content}, #{registerDate}, #{modifyDate});
   </insert>
   
</mapper>